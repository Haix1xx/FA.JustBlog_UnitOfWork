@using FA.JustBlog.Core.Models;
@model IEnumerable<Tag>
<div class="card">
    <h5 class="card-header">Popular tags</h5>
    <div class="card-body">
        @foreach (var tag in Model)
        {
            <a class="btn btn-outline-info border-2 p-2 m-1">@tag.Name</a>
        }
    </div>
</div>